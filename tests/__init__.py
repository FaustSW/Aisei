"""
Test Package

This directory will contain automated tests for the application.

Planned focus areas:
- Scheduler behavior (SM-2 integration via scheduler_adapter)
- Review workflow logic (ReviewState selection and rating updates)
- Queue classification and card ordering (queue_service)
- Statistics computation (stats_service metrics)
- Authentication logic (password hashing and verification)

Tests will primarily target the service layer to verify business logic
independently of the web (Flask) layer and external API clients.
"""
